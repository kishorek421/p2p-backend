import LoadingBar from "@/components/LoadingBar";
import { View } from "react-native";

const IndexPage = () => {
  return (
    <View>
      <LoadingBar />
    </View>
  );
};

export default IndexPage;

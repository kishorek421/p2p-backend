export enum GeoLocationType {
  PINCODE,
  AREA,
  CITY,
  STATE,
  COUNTRY,
}

export enum OrgDropdownType {
  ORGANIZATION,
  DEPARTMENT,
  DESIGNATION,
  BRANCH,
}

export enum TextCase {
  uppercase,
  lowercase,
  freeform,
}

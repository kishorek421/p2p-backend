export enum GeoLocationType {
  PINCODE,
  AREA,
  CITY,
  STATE,
  COUNTRY,
}

export enum OrgDropdownType {
  ORGANIZATION,
  DEPARTMENT,
  DESIGNATION,
}

export enum TextCase {
  uppercase,
  lowercase,
  freeform,
}
